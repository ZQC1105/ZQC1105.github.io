# L0单元测试：哪些需要，哪些不要，以及最佳实践

## 🎯 L0单元测试的核心定位

**单元测试只测试一件事：一个代码单元（通常是一个类/方法）的内部逻辑是否正确，不依赖任何外部资源。**

---

## ✅ **必须写单元测试的10种场景**

### 1. **核心业务逻辑/领域模型**
```csharp
// ✅ 必须测试：业务规则计算
public class PriceCalculator
{
    public decimal CalculateDiscount(Customer customer, decimal amount)
    {
        if (customer.IsVIP) return amount * 0.8m;  // VIP 8折
        if (amount > 1000) return amount * 0.9m;   // 大额订单9折
        return amount;
    }
}

// 对应的单元测试
[Fact]
public void CalculateDiscount_VIPCustomer_Returns20PercentOff()
{
    // Arrange
    var calculator = new PriceCalculator();
    var vipCustomer = new Customer { IsVIP = true };
    
    // Act
    var result = calculator.CalculateDiscount(vipCustomer, 100);
    
    // Assert
    Assert.Equal(80, result);  // 100 * 0.8 = 80
}
```

### 2. **算法和复杂计算**
```csharp
// ✅ 必须测试：复杂算法
public class GeoLocationCalculator
{
    public double CalculateDistance(Location point1, Location point2)
    {
        // 哈弗辛公式计算球面距离
        var lat1 = ToRadians(point1.Latitude);
        var lon1 = ToRadians(point1.Longitude);
        var lat2 = ToRadians(point2.Latitude);
        // ... 复杂计算逻辑
    }
}
// 这种有明确数学公式的方法必须测试边界情况
```

### 3. **验证逻辑和输入校验**
```csharp
// ✅ 必须测试：输入验证
public class UserRegistrationValidator
{
    public ValidationResult Validate(UserRegistrationRequest request)
    {
        var errors = new List<string>();
        
        if (string.IsNullOrEmpty(request.Email))
            errors.Add("Email is required");
        else if (!IsValidEmail(request.Email))
            errors.Add("Invalid email format");
            
        if (request.Password?.Length < 8)
            errors.Add("Password must be at least 8 characters");
            
        return new ValidationResult(errors);
    }
}

// 测试多种边界情况
[Theory]
[InlineData(null, "1234567", new[] { "Email is required", "Password must be at least 8 characters" })]
[InlineData("invalid-email", "password123", new[] { "Invalid email format" })]
[InlineData("valid@email.com", "password123", new string[] { })]
public void Validate_Returns_Correct_Errors(string email, string password, string[] expectedErrors)
{
    // 测试各种组合
}
```

### 4. **状态机/状态转换**
```csharp
// ✅ 必须测试：状态流转
public class Order
{
    public OrderStatus Status { get; private set; }
    
    public void Approve()
    {
        if (Status != OrderStatus.Pending)
            throw new InvalidOperationException("Only pending orders can be approved");
        
        Status = OrderStatus.Approved;
    }
    
    public void Cancel()
    {
        if (Status == OrderStatus.Shipped || Status == OrderStatus.Delivered)
            throw new InvalidOperationException("Cannot cancel shipped or delivered orders");
        
        Status = OrderStatus.Cancelled;
    }
}

// 测试所有有效的状态转换
```

### 5. **工具类和扩展方法**
```csharp
// ✅ 必须测试：通用工具方法
public static class StringExtensions
{
    public static string Truncate(this string str, int maxLength, string suffix = "...")
    {
        if (string.IsNullOrEmpty(str) || str.Length <= maxLength)
            return str;
            
        return str.Substring(0, maxLength - suffix.Length) + suffix;
    }
}

[Theory]
[InlineData("Hello World", 5, "...", "He...")]
[InlineData("Hello", 10, "...", "Hello")]
[InlineData(null, 5, "...", null)]
public void Truncate_Returns_Expected(string input, int maxLength, string suffix, string expected)
{
    // 测试各种边界
}
```

### 6. **策略模式实现**
```csharp
// ✅ 必须测试：策略算法的不同实现
public interface IDiscountStrategy
{
    decimal CalculateDiscount(decimal amount);
}

public class VIPDiscountStrategy : IDiscountStrategy
{
    public decimal CalculateDiscount(decimal amount) => amount * 0.8m;
}

public class BulkDiscountStrategy : IDiscountStrategy
{
    public decimal CalculateDiscount(decimal amount) => amount > 1000 ? amount * 0.9m : amount;
}

// 每个策略单独测试
```

### 7. **映射器/转换器**
```csharp
// ✅ 必须测试：对象映射逻辑
public class UserMapper
{
    public UserDto MapToDto(User user)
    {
        return new UserDto
        {
            Id = user.Id,
            FullName = $"{user.FirstName} {user.LastName}",
            Age = DateTime.Now.Year - user.BirthDate.Year,
            IsActive = user.Status == UserStatus.Active
        };
    }
}

// 测试映射是否正确处理了所有字段
```

### 8. **条件分支和边缘情况**
```csharp
// ✅ 必须测试：所有分支路径
public class TaxCalculator
{
    public decimal CalculateTax(decimal income, TaxRegion region)
    {
        if (income <= 0) return 0;
        
        switch (region)
        {
            case TaxRegion.US:
                return income <= 50000 ? income * 0.1m : income * 0.2m;
            case TaxRegion.EU:
                return income <= 30000 ? income * 0.15m : income * 0.25m;
            case TaxRegion.ASIA:
                return income * 0.05m;
            default:
                throw new ArgumentException("Invalid region");
        }
    }
}

// 测试：收入为0、负数、各种区域、边界值（50000、30000）
```

### 9. **配置/选项验证**
```csharp
// ✅ 必须测试：配置验证逻辑
public class EmailServiceOptionsValidator
{
    public void Validate(EmailServiceOptions options)
    {
        if (options == null)
            throw new ArgumentNullException(nameof(options));
            
        if (string.IsNullOrEmpty(options.SmtpServer))
            throw new ArgumentException("SMTP server is required");
            
        if (options.Port <= 0 || options.Port > 65535)
            throw new ArgumentException("Invalid port number");
            
        if (options.MaxRetries < 0)
            throw new ArgumentException("Max retries cannot be negative");
    }
}
```

### 10. **错误处理和异常路径**
```csharp
// ✅ 必须测试：异常情况
public class FileProcessor
{
    public string ProcessFile(string filePath)
    {
        if (!File.Exists(filePath))
            throw new FileNotFoundException($"File not found: {filePath}");
            
        if (new FileInfo(filePath).Length > 10 * 1024 * 1024) // 10MB
            throw new InvalidOperationException("File too large");
            
        // 处理文件...
    }
}

// 测试：文件不存在、文件过大等异常情况
```

---

## ❌ **不需要写单元测试的8种场景**

### 1. **简单属性/自动属性**
```csharp
// ❌ 不需要测试：简单的getter/setter
public class User
{
    public int Id { get; set; }          // 自动属性，不需要测试
    public string Name { get; set; }     // 自动属性，不需要测试
    public DateTime CreatedAt { get; }   // 只读属性，通常也不需要
}
```

### 2. **简单的DTO/ViewModel**
```csharp
// ❌ 不需要测试：纯数据容器
public class UserDto
{
    public int Id { get; set; }
    public string Name { get; set; }
    public string Email { get; set; }
}
// 没有逻辑，只是数据传输
```

### 3. **委托/直接调用的外部方法**
```csharp
// ❌ 不需要单元测试：直接调用第三方库
public class Logger
{
    public void Log(string message)
    {
        // 直接调用外部库
        Console.WriteLine(message);  // 不应该测试Console.WriteLine
        File.AppendAllText("log.txt", message);  // 不应该测试File系统
    }
}
// 这些应该在集成测试中测试
```

### 4. **配置类/启动代码**
```csharp
// ❌ 不需要测试：启动配置
public class Startup
{
    public void ConfigureServices(IServiceCollection services)
    {
        services.AddControllers();
        services.AddDbContext<AppDbContext>();  // 这是框架配置，不需要测试
    }
}
```

### 5. **空方法/模板方法**
```csharp
// ❌ 不需要测试：空实现或待实现的方法
public abstract class BaseService
{
    public virtual void Initialize() { }  // 空方法，不需要测试
    
    public abstract void Process();       // 抽象方法，在子类中测试
}
```

### 6. **纯UI代码（如果前后端分离）**
```csharp
// ❌ 通常不需要单元测试：MVC的Controller方法（如果只是简单的转发）
[ApiController]
public class UsersController : ControllerBase
{
    private readonly IUserService _userService;
    
    [HttpGet("{id}")]
    public async Task<ActionResult<UserDto>> GetUser(int id)
    {
        // 简单转发到Service层
        var user = await _userService.GetUserAsync(id);
        return Ok(user);  // 逻辑在Service层，这里不需要单元测试
    }
}
// 注：如果Controller有复杂逻辑（如权限检查、数据转换），则需要测试
```

### 7. **外部API/第三方SDK包装器（简单包装）**
```csharp
// ❌ 不需要单元测试：简单包装
public class PaymentGatewayClient
{
    private readonly IPaymentGateway _gateway;
    
    public async Task<PaymentResult> ProcessPayment(PaymentRequest request)
    {
        // 直接转发到第三方SDK
        return await _gateway.ProcessAsync(request);
    }
}
// 应该在集成测试中测试与真实服务的交互
```

### 8. **AutoMapper配置（如果只是简单映射）**
```csharp
// ❌ 如果只是简单字段映射，不需要单元测试
CreateMap<User, UserDto>()
    .ForMember(dest => dest.FullName, opt => opt.MapFrom(src => src.FirstName + " " + src.LastName))
    .ForMember(dest => dest.Age, opt => opt.MapFrom(src => CalculateAge(src.BirthDate)));
// 但如果有复杂的CalculateAge逻辑，那部分需要单独测试
```

---

## 🎪 **需要判断的灰色地带**

### 情况1：**Repository模式的数据访问层**
```csharp
public class UserRepository : IUserRepository
{
    private readonly AppDbContext _context;
    
    public async Task<User> GetByIdAsync(int id)
    {
        // ❓ 是否测试？
        // 如果只是简单的DbContext调用，单元测试意义不大
        return await _context.Users.FindAsync(id);
    }
    
    public async Task<User> GetActiveUsersByDateAsync(DateTime date)
    {
        // ✅ 如果有复杂查询逻辑，应该测试
        return await _context.Users
            .Where(u => u.Status == UserStatus.Active)
            .Where(u => u.CreatedAt >= date)
            .OrderByDescending(u => u.LastLogin)
            .ToListAsync();
    }
}
```

**建议**：简单的CRUD不需要单元测试（用集成测试），复杂查询逻辑需要单元测试。

### 情况2：**Service层中的业务逻辑**
```csharp
public class OrderService
{
    public async Task<OrderResult> PlaceOrder(OrderRequest request)
    {
        // ✅ 验证逻辑需要测试
        ValidateOrder(request);
        
        // ✅ 业务规则需要测试
        var discount = CalculateDiscount(request.CustomerId, request.Total);
        
        // ❌ 保存到数据库应该在集成测试中测试
        var order = new Order { /* ... */ };
        await _repository.SaveAsync(order);
        
        // ❌ 发送邮件应该在集成测试中测试
        await _emailService.SendOrderConfirmation(order);
        
        return new OrderResult { Success = true };
    }
}
```

**建议**：测试纯业务逻辑部分，Mock外部依赖。

---

## 📊 **单元测试覆盖优先级矩阵**

| 代码类型              | 复杂度 | 业务重要性 | 测试优先级   | 建议                   |
| --------------------- | ------ | ---------- | ------------ | ---------------------- |
| 价格计算器            | 高     | 高         | 🔴 **最高**   | 必须测试，边界值全覆盖 |
| 用户验证器            | 中     | 高         | 🔴 **最高**   | 测试所有验证规则       |
| 订单状态机            | 中     | 高         | 🔴 **高**     | 测试所有状态转换       |
| API Controller        | 低     | 中         | 🟡 **中低**   | 只测有逻辑的部分       |
| Repository (简单CRUD) | 低     | 中         | 🟢 **低**     | 集成测试即可           |
| DTO/ViewModel         | 低     | 低         | ⚪ **不需要** | 不测试                 |

---

## 🔧 **实用的单元测试编写策略**

### 策略1：**测试驱动开发（TDD）核心逻辑**
```csharp
// 1. 先写测试
[Fact]
public void CalculateTotal_WithTax_ReturnsCorrectAmount()
{
    // Arrange
    var calculator = new OrderCalculator();
    
    // Act
    var result = calculator.CalculateTotal(100, 0.1m); // 100 + 10%税
    
    // Assert
    Assert.Equal(110, result);
}

// 2. 实现最简单的代码让测试通过
public class OrderCalculator
{
    public decimal CalculateTotal(decimal subtotal, decimal taxRate)
    {
        return subtotal * (1 + taxRate);
    }
}
```

### 策略2：**为Bug修复添加测试**
```csharp
// 当发现一个Bug时：
// 1. 先写一个重现Bug的测试
[Fact]
public void CalculateDiscount_Bug_ZeroAmount_ShouldNotThrow()
{
    // 之前：amount=0时会抛出除零异常
    var calculator = new PriceCalculator();
    
    var result = calculator.CalculateDiscount(new Customer(), 0);
    
    Assert.Equal(0, result);  // 应该返回0，而不是异常
}

// 2. 修复Bug
// 3. 确保测试通过
```

### 策略3：**使用测试金字塔指导测试数量**
```
🔺 单元测试 (70-80%) - 快速、隔离、测试业务逻辑
   ⬇
🔺🔺 集成测试 (15-20%) - 测试组件协作
   ⬇
🔺🔺🔺 E2E测试 (5-10%) - 测试完整流程
```

---

## 🚀 **实际项目中的单元测试检查清单**

### **必须覆盖的核心（每个项目都应该有）**
- [ ] **领域模型的业务规则**（价格计算、折扣策略、验证规则）
- [ ] **工具类和扩展方法**（字符串处理、日期计算、格式转换）
- [ ] **配置验证**（选项类、设置验证）
- [ ] **错误处理逻辑**（异常抛出条件）
- [ ] **状态转换逻辑**（状态机的有效转换）

### **根据项目类型决定**
- [ ] **API参数绑定和验证**（如果有复杂绑定逻辑）
- [ ] **权限检查中间件**（如果有自定义权限逻辑）
- [ ] **缓存策略实现**（如果有复杂缓存逻辑）
- [ ] **消息队列处理器**（如果有消息处理逻辑）

### **通常不需要**
- [ ] **简单的CRUD操作**
- [ ] **第三方库的直接调用**
- [ ] **框架配置代码**
- [ ] **UI/视图相关的代码**

---

## 💡 **经验法则**

1. **测试你的逻辑，而不是框架的逻辑**
2. **如果一个方法没有分支/条件/计算，可能不需要单元测试**
3. **测试行为，而不是实现细节**
4. **优先测试核心业务逻辑和容易出错的部分**
5. **使用集成测试覆盖外部依赖和组件协作**

记住：**单元测试的目的是让你有信心重构代码**。如果你发现修改一个实现细节导致多个单元测试失败，说明你的测试耦合了实现细节，而不是测试行为。

最有效的单元测试是那些**测试"什么"（What）而不是"如何"（How）**的测试。